# Production environment variables
# VITE_API_URL MUST be relative (/api) so the frontend inherits HTTPS from the page origin.
# NEVER set an absolute http:// URL here — it causes Mixed Content errors.
VITE_API_URL=/api
